export interface Prompt {
    text: string; // The actual prompt text (e.g., "I’d like a North African inspired beef dinner...")
    sessionId: string; // The session ID associated with this prompt
    timestamp: string; // When the prompt was created
  }